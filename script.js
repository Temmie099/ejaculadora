function somar(){
     var somar;
     var x, y;
     x= document.getElementById(x).value;
     y= document.getElementById(y).value;
     somar=parseInt(x)+parseInt(y);
     alert(somar);
}
function subtrair(){
    var subtrair;
    var x, y;
    x= document.getElementById(x).value;
    y= document.getElementById(y).value;
    subtrair=parseInt(x)-parseInt(y);
    alert(subtrair);
}
function multiplicar(){
    var multiplicar;
    var x, y;
    x= document.getElementById(x).value;
    y= document.getElementById(y).value;
    multiplicar=parseInt(x)*parseInt(y);
    alert(multiplicar);
}
function dividir(){
    var dividir;
    var x, y;
    x= document.getElementById(x).value;
    y= document.getElementById(y).value;
    dividir=parseInt(x)/parseInt(y);
    alert(dividir);
}